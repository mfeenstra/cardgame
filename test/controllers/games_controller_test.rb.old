require "test_helper"

class GamesControllerTest < ActionDispatch::IntegrationTest
  test "the truth" do
    assert true
  end

  test "it should get the index:" do
    get games_url
    assert_response success
  end

end
